rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Overly permissive rules for development - allow all authenticated users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}